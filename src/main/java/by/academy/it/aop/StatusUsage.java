package by.academy.it.aop;

public class StatusUsage {
    private TransferService.Status ok = TransferService.Status.OK;
    private TransferService.Status err = TransferService.Status.ERROR;
    private TransferService.Status no = TransferService.Status.NOT_ENOUGH_FUNDS;
}
